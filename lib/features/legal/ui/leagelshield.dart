import 'package:flutter/material.dart';

class Leagelshield extends StatelessWidget {
  const Leagelshield({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}